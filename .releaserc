{
  "tagFormat": "${version}",
  "branches": [
    "master",
    { "name": "feat/*", "channel": "preview", "prerelease": "${name.replace(/^feat\\//g, 'feat-')}" },
    { "name": "fix/*", "channel": "preview", "prerelease": "${name.replace(/^fix\\//g, 'fix-')}" },
    { "name": "chore/*", "channel": "preview", "prerelease": "${name.replace(/^chore\\//g, 'chore-')}" }
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/changelog", {
      "changelogFile": "CHANGELOG.md"
    }],
    "@semantic-release/npm",
    ["@semantic-release/git", {
      "assets": ["CHANGELOG.md", "docs", "package.json", "pnpm-lock.yaml"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    "@semantic-release/github"
  ]
}
